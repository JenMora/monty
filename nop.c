#include "monty.h"

/**
 * nop - Wow, this function just does nothing
 * @stack: element at the top of the stack (head)
 * @mj_count: line count of the nop command in the monty file
 * Authors: Magdalene and Jeniffer
 * Return: 0, otherwise EXIT FAILURE
 */
void nop(mj_stack **stack, unsigned int mj_count)
{
	(void)stack;
	(void)mj_count;
}
